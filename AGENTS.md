# Agent Guidelines

- Batch scripts in this repository must start with `@echo off` and use Windows-style `CRLF` line endings.
- Use `REM` for inline comments inside batch files.
- Document installer or uninstaller changes in `README.md` under an "Installation" or similar section.
